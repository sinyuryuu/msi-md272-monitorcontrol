# MSI MD272UPH / MD272UPHW 特製版技術文件

本文件記錄這個 MonitorControl 特製版針對 MSI MD272UPH / MD272UPHW 所做的修改、硬體行為結論、控制原理與維護注意事項。

## 目標

讓 MSI MD272UPH / MD272UPHW 在 macOS 上可以正常使用：

- 螢幕硬體音量調整
- 靜音 / 取消靜音
- 亮度調整
- 對比度調整
- 鍵盤音量鍵與亮度鍵直接控制螢幕
- 自訂 HUD 顯示「音量」與「亮度」

## 硬體測試結論

這台螢幕的控制介面比較特殊，不是單純 DDC/CI 就能全部處理。

### 目前穩定版本狀態

目前可正常工作的重點：

- 鍵盤音量鍵可直接控制螢幕硬體音量。
- 鍵盤亮度鍵可直接控制螢幕 DDC 亮度。
- 自訂 HUD 會顯示繁體中文 `音量` / `亮度`。
- 打開 menu bar 面板時，亮度 slider 會讀取螢幕實際 DDC 值。
- 切換來源後回到 Type-C，會延遲同步實體亮度，避免 UI 使用舊偏好值。
- MSI 這台會略過 MonitorControl 原本的 combined brightness 軟體/硬體分段邏輯，避免啟動或重配置時把實體 DDC 亮度拉低。
- HID 離線時，螢幕設定與 PIP/PBP 選單不再長時間阻塞；會顯示 `MSI HID 未連線`。
- PIP/PBP 關閉時，`切換顯示` / `切換音訊` 會停用。

### DDC/CI 可用項目

| 功能 | VCP Code | 結論 |
| --- | --- | --- |
| 亮度 | `0x10` | 可讀、可寫，正常生效 |
| 對比度 | `0x12` | 可讀、可寫，正常生效 |
| 音量 | `0x62` | 可讀目前值，但寫入不會套用 |
| 靜音 | `0x8D` | 可讀、可寫，但語意和 MonitorControl 預期相反 |

### 靜音值反轉

MSI MD272 的 DDC 靜音值和一般邏輯相反：

| MSI raw value | 實際狀態 |
| --- | --- |
| `1` | 取消靜音 |
| `2` | 靜音 |

因此程式內對 `audioMuteScreenBlank` 做了讀寫轉換：

- 讀取時把 MSI raw value 轉成 MonitorControl 內部慣用值
- 寫入時再轉回 MSI raw value

### 音量不能用 DDC 寫入

DDC `0x62` 可以查到目前音量，例如讀回 `55`，但 set 到 `60` 後螢幕實際音量不變，重新讀也仍是舊值。

最後確認音量必須透過 USB HID 控制。

### HID 音量控制

螢幕會提供一個 USB HID 裝置：

- 裝置名稱：`MSI Gaming Controller`
- VID/PID：`0x1462:0x3fa4`
- 可用命令：`00870`

專案內建 helper：

```text
MonitorControl/Support/msi-hid-volume-set
```

MonitorControl 寫入音量時，會呼叫這個 helper 透過 HID 設定螢幕硬體音量。

### HID OSD 控制

除音量外，MSI OSD 設定主要也透過 HID 控制。專案內建 helper：

```text
MonitorControl/Support/msi-hid-input-set
```

已確認可讀取或控制的命令：

| 功能 | HID command | 值 |
| --- | --- | --- |
| 輸入來源 | `00500` | `0=HDMI 1`, `1=HDMI 2`, `2=DP`, `3=Type-C` |
| 自動掃描來源 | `00510` | `0=關閉`, `1=開啟` |
| 螢幕模式 | `00300` | `0=節能`, `1=使用者`, `2=sRGB`, `3=Display P3`, `4=抗藍光`, `5=電影`, `6=辦公室`, `7=黑白模式` |
| Eye Saver | `00310` | `0=關閉`, `1=開啟` |
| Response Time | `00220` | `0=Normal`, `1=Fast`, `2=Fastest` |
| Screen Size | `002:0` | `0=Auto`, `1=4:3`, `2=16:9` |
| KVM | `008>0` | `0=自動`, `1=USB 上行`, `2=Type-C` |
| OSD Timeout | `00820` | `5`, `10`, `15`, `20`, `30` 秒 |
| HDMI CEC | `008:0` | `0=關閉`, `1=開啟` |
| PIP/PBP 模式 | `00600` | `0=關閉`, `1=PIP`, `2=PBP` |
| PIP 輸入 | `00610` | `0=HDMI 1`, `1=HDMI 2`, `2=DP`, `3=Type-C` |
| PBP 輸入 | `00620` | `0=HDMI 1`, `1=HDMI 2`, `2=DP`, `3=Type-C` |
| PIP 大小 | `00630` | `0=Small`, `1=Medium`, `2=Large` |
| PIP 位置 | `00640` | `0=左上`, `1=右上`, `2=左下`, `3=右下` |
| 切換顯示 | `00650` | action，PIP/PBP 模式下才啟用 |
| 切換音訊 | `00660` | action，PIP/PBP 模式下才啟用 |

注意：當螢幕來源不是 Type-C 時，macOS 仍可能看到顯示器是 Online，但 USB HID 介面會消失。此時 HID OSD 功能應停用或顯示離線，不應反覆等待 helper timeout。

## 主要修改檔案

### `MonitorControl/Support/MSIMD272SpecialControl.swift`

新增 MSI MD272 專用控制邏輯，是本特製版核心。

主要內容：

- 判斷目前 display 是否為 MSI MD272 特殊螢幕
- HID 音量設定包裝
- DDC 靜音值讀寫轉換
- 啟動與選單開啟時讀取實際亮度
- 鍵盤亮度鍵直接走 DDC raw brightness
- 自訂 HUD 顯示音量 / 亮度
- 低階 `CGEventTap` 攔截鍵盤媒體鍵
- 除錯 log 輸出到 `/tmp/msi-md272-monitorcontrol.log`

### `MonitorControl/Model/OtherDisplay.swift`

修改一般外接螢幕控制流程，讓 MSI MD272 走特殊路徑。

主要修改：

- 音量 `.audioSpeakerVolume` 寫入時，不走 DDC，改呼叫 HID helper
- 靜音 `.audioMuteScreenBlank` 讀寫時套用 MSI 反轉邏輯
- 鍵盤或 slider 觸發亮度時，MSI 走專用 DDC raw brightness 流程
- 音量、靜音、亮度變更後顯示自訂 HUD
- MSI MD272 不使用 combined brightness 分段換算，避免內部值被換算成過低的 DDC raw brightness

### `MonitorControl/Support/MenuHandler.swift`

修正 menu bar icon 打開後亮度面板預設顯示 `100%` 的問題。

原因是原本 menu slider 建立時可能只拿到預設值或舊偏好值。現在在選單開啟前會先呼叫：

```swift
display.refreshMSIMD272BrightnessForMenuIfAvailable()
```

並在建立亮度 slider 前再次同步硬體亮度。這個方法會先確認 HID 目前來源是 Type-C，避免螢幕切到 HDMI/DP 時因 HID 不存在而阻塞選單。

同時新增螢幕設定與 PIP/PBP 選單：

- 輸入來源切換：HDMI 1 / HDMI 2 / DP / Type-C
- 螢幕設定：自動掃描來源、螢幕模式、Eye Saver、Response Time、Screen Size、KVM、OSD Timeout、HDMI CEC
- PIP/PBP：模式、輸入、大小、位置、切換顯示、切換音訊
- HID 離線時顯示 `MSI HID 未連線`

### `MonitorControl/Support/AppDelegate.swift`

加入 MSI media key interceptor 啟動與延遲重試。

因為 Accessibility 權限、螢幕重掃描、睡眠喚醒都可能影響 event tap，所以啟動後會在數個時間點 retry。

另加入 MSI 亮度延遲同步：

- configure 完成後延遲約 `0.8` 秒與 `2.5` 秒讀取實體亮度
- 只在 HID 來源確認為 Type-C 時同步
- 解決切換訊號源後回到 Type-C，menu 或鍵盤邏輯仍使用舊亮度值的問題

### `MonitorControl/Support/DisplayManager.swift`

`restoreSwBrightnessForAllDisplays` 會略過 MSI MD272 特殊螢幕。

原因：MonitorControl 原本 combined brightness 會在某些狀態恢復軟體遮罩 / 分段亮度值，對這台 MSI 會造成啟動後實體 DDC 亮度被拉低，例如從 `100` 變成 `19`。MSI 特製版改成硬體 DDC 亮度直接控制，不使用這段恢復流程。

### `MonitorControl/Support/MediaKeyTapManager.swift`

保留原本 MonitorControl 的 media key 流程，但針對 MSI 特殊螢幕做偏好處理。

目前真正接管鍵盤音量 / 亮度鍵的是 `MSIMD272MediaKeyInterceptor` 的低階 `CGEventTap`。

### `MonitorControl/Support/SliderHandler.swift`

小幅調整 slider 顯示與值同步，確保程式主動讀取亮度後，menu slider 可反映目前值。

## 控制原理

### 音量

流程：

1. 使用者按鍵盤音量鍵，或在 MonitorControl 調整音量
2. MSI 專用邏輯攔截 `.audioSpeakerVolume`
3. 將 0 到 1 的內部值轉為 0 到 100
4. 呼叫 `msi-hid-volume-set`
5. helper 透過 USB HID 對 `MSI Gaming Controller` 下命令
6. 螢幕硬體音量實際改變
7. 顯示自訂 HUD「音量」

注意：macOS 原生音量 HUD 不一定能正確反映螢幕硬體音量，因為實際控制不是 CoreAudio 系統音量，而是螢幕 HID 硬體音量。

### 靜音

流程：

1. 使用者按鍵盤靜音鍵或在 UI 切換
2. MonitorControl 內部仍使用原本靜音 command
3. MSI 特製邏輯將值反轉
4. 透過 DDC `0x8D` 寫入螢幕
5. 顯示自訂 HUD「音量」或「靜音」

### 亮度

流程：

1. 啟動或打開 menu bar icon 面板時，先讀 DDC `0x10`
2. 將 raw current/max 轉成 0 到 1 的內部值
3. 更新偏好值、同步來源值、menu slider
4. 鍵盤亮度鍵調整時，先讀目前 raw brightness
5. 計算下一格
6. 透過 DDC `0x10` 寫回
7. 顯示自訂 HUD「亮度」

這樣可以避免啟動時亮度面板永遠顯示 `100%`。

重要：MSI 這台不使用 MonitorControl 原本的 combined brightness。原本邏輯會把內部亮度分成軟體遮罩與硬體 DDC 兩段，例如內部 `0.6875` 可能被換算成 DDC raw `19`，導致畫面突然變暗。現在 MSI MD272 直接使用 DDC raw `0` 到 `100`。

### 對比度

對比度仍走 MonitorControl 原本的 DDC 流程，使用 VCP `0x12`。

## 自訂 HUD

原生 macOS 音量 HUD 無法可靠顯示螢幕 HID 音量，因此加入自訂 HUD。

目前 HUD 設計：

- 顯示語言：繁體中文
- 標題：`音量` / `亮度`
- 深黑半透明背景
- 白色文字
- 16 格進度條
- 靜音時顯示 `靜音`

曾經使用 `NSVisualEffectView`，但在部分背景下底色太白，白色文字可讀性不足。現在改成一般 `NSView` 搭配黑色半透明 layer，避免毛玻璃效果造成對比不穩定。

## 權限注意事項

鍵盤音量鍵與亮度鍵攔截需要 macOS Accessibility 權限。

如果 `/tmp/msi-md272-monitorcontrol.log` 出現：

```text
trusted=false
```

代表目前 app 沒有輔助使用權限，需要到：

```text
系統設定 > 隱私權與安全性 > 輔助使用
```

允許 `MSIMonitorControl`。

注意：因為測試版曾經使用不同 bundle id，例如：

```text
com.sinyuryuu.MSIMonitorControl.v16
```

每換一個 bundle id，macOS 都可能要求重新授權。

目前固定版已改為：

```text
Bundle ID: com.sinyuryuu.MSIMonitorControl
App path: outputs/MSI-MonitorControl.app
Signing authority: MSI MonitorControl Local Code Signing
```

只要後續打包保持同一個 bundle id、同一個 app 路徑、同一張本機簽章，Accessibility 權限通常不需要每版重新授權。

## 除錯方式

主要 log：

```text
/tmp/msi-md272-monitorcontrol.log
```

常用檢查：

```sh
cat /tmp/msi-md272-monitorcontrol.log
```

重啟測試：

```sh
rm -f /tmp/msi-md272-monitorcontrol.log
open outputs/MSI-MonitorControl.app
sleep 5
cat /tmp/msi-md272-monitorcontrol.log
```

重點 log：

| log | 意義 |
| --- | --- |
| `brightness refresh current=... internal=...` | 已讀到螢幕實際亮度 |
| `interceptor update shouldRun=true trusted=true` | 鍵盤攔截可啟動 |
| `trusted=false` | 尚未授權輔助使用 |
| `hid volume set value=...` | HID 音量設定流程有被呼叫 |
| `brightness key raw current=... next=...` | 鍵盤亮度鍵正在用 DDC raw brightness 調整 |

## 建置方式

Release build 範例：

```sh
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
xcodebuild -quiet \
  -project MonitorControl.xcodeproj \
  -scheme MonitorControl \
  -configuration Release \
  -derivedDataPath /tmp/MonitorControl-MSI-DirectKeys \
  -destination 'platform=macOS,arch=arm64' \
  -skipMacroValidation \
  -skipPackagePluginValidation \
  CODE_SIGNING_ALLOWED=NO \
  CODE_SIGNING_REQUIRED=NO \
  CODE_SIGN_IDENTITY='' \
  DEVELOPMENT_TEAM=''
```

## 固定簽章與包裝

早期測試包使用 ad-hoc signing：

```sh
codesign --force --deep --sign - MSI-MonitorControl-DirectKeys.app
codesign --verify --deep --strict --verbose=2 MSI-MonitorControl-DirectKeys.app
```

這會導致每次重新打包後 CodeDirectory hash 改變，macOS TCC / Accessibility 可能把它視為不同程式，造成每版都要重新授權。

目前改用專案專用 keychain 與固定本機簽章：

```text
Keychain: work/msi-monitorcontrol-signing.keychain-db
Keychain password: msi-monitorcontrol-local-signing
Identity SHA-1: B12AB52649E27EB3291736D21F2B54836B327A3A
Authority: MSI MonitorControl Local Code Signing
Bundle ID: com.sinyuryuu.MSIMonitorControl
Output app: outputs/MSI-MonitorControl.app
```

簽章前需確保專案 keychain 在 user search list，且已 unlock：

```sh
KEYCHAIN="$PWD/../msi-monitorcontrol-signing.keychain-db"
security unlock-keychain -p 'msi-monitorcontrol-local-signing' "$KEYCHAIN"
```

簽章範例：

```sh
codesign --force --deep \
  --sign B12AB52649E27EB3291736D21F2B54836B327A3A \
  outputs/MSI-MonitorControl.app

codesign --verify --deep --strict --verbose=2 \
  outputs/MSI-MonitorControl.app
```

不要加 `--options runtime`，否則 Sparkle framework 可能因 hardened runtime library validation 與本機簽章 Team ID 不一致而無法載入。

## 維護注意事項

- 不要把 MSI 音量改回 DDC `0x62` 寫入；此螢幕會回報但不套用。
- 靜音 `0x8D` 的值必須保持反轉轉換。
- 亮度 menu slider 必須在開啟面板前讀取螢幕實際值，不然會回到預設 `100%`。
- MSI MD272 不應走 combined brightness，否則啟動或重配置時可能把 DDC 亮度拉低。
- 自訂 HUD 不建議改回 `NSVisualEffectView`，容易在淺色背景下可讀性不足。
- 每次改 bundle id、app 路徑或簽章 identity 後，macOS Accessibility 權限可能需要重新授權。
- 固定版請使用 `outputs/MSI-MonitorControl.app`、`com.sinyuryuu.MSIMonitorControl` 與 `MSI MonitorControl Local Code Signing`。
- HID helper 必須被包進 app resources，否則音量設定會失敗。
- HID OSD helper 必須被包進 app resources，否則輸入來源、PIP/PBP、螢幕設定會失敗。
- 若未來要做穩定發行版，建議固定 bundle id，避免使用者每版都重新授權。

## 目前 GitHub 倉庫

```text
https://github.com/sinyuryuu/msi-md272-monitorcontrol
```

目前主要分支：

```text
main
```

本地開發分支：

```text
msi-md272-special
```
